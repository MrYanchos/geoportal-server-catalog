(function() {
    // window.keycloak = Keycloak("custom/keycloak.json");
    // window.keycloak.init().success(function(authenticated) {
    //     alert(authenticated ? 'authenticated' : 'not authenticated');
    // }).error(function() {
    //     alert('failed to initialize');
    // });
    // Custom initialization

})();